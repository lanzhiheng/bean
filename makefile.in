CC = gcc -v
TARGET = bean
CFLAGS = -g -Wall
TFLAGS = -g -Wall -DTEST
CFILES = $(wildcard *.c)
CURL = @LIBCURL@ @LIBCURL_CPPFLAGS@

$(TARGET):$(OBJS)
	$(CC) -o $(TARGET) $(CFILES) $(CFLAGS) $(LIBS) $(CURL)

test:
	$(CC) -o $(TARGET) $(CFILES) $(TFLAGS) $(LIBS)
	./$(TARGET)

clean:
	-$(RM) $(TARGET)
