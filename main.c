#include <stdio.h>
#include <stdlib.h>
/* #include "parser.h" */

void hello(const char * c) {
  /* printf("%s\n", c); */
  /* printf("%lu", sizeof((c))); */
  printf("%lu", sizeof(size_t));
}

int main() {
  printf("%lu", sizeof(size_t));
}
